- id: chmod
  name: change file permissions
  entry: one_liners chmod
  types: [file]
  files: ^$
  language: python

- id: regex
  name: regular expression search and replace
  entry: one_liners regex
  types: [file]
  files: ^$
  language: python
